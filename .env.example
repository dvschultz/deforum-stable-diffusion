# Copy this file to `.env` and fill in your fal.ai API key.
# Generation runs on the hosted Z-Image Turbo model; the key is read from the
# environment (a real FAL_KEY env var always takes precedence over this file).
# `.env` is gitignored so your key is never committed.
FAL_KEY=your-fal-api-key-here
